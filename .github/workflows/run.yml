name: build

on: [push]

jobs:
  cowsay:
    runs-on: self-hosted
    container: npalm/cowsay

    steps:
      - uses: actions/checkout@v1
      - name: build
        run: |
          sleep 90
          cowsay -f ghostbusters "    Who ya gonna call?" > cow.txt
          cat cow.txt
